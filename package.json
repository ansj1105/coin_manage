{
  "name": "korion-kori-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down -v",
    "db:migrate": "docker compose run --rm flyway migrate",
    "db:validate": "docker compose run --rm flyway validate",
    "db:info": "docker compose run --rm flyway info",
    "stack:up": "docker compose up -d --build",
    "stack:down": "docker compose down",
    "stack:logs": "docker compose logs -f app-api app-withdraw-worker app-ops"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.883.0",
    "bullmq": "^5.71.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "ioredis": "^5.10.0",
    "kysely": "^0.28.11",
    "pg": "^8.13.3",
    "tronweb": "^6.2.2",
    "uuid": "^11.0.5",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.10",
    "@types/pg": "^8.11.11",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
